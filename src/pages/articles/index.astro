---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const articles = (await getCollection('articles'))
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime());

function fmtDate(d: Date): string {
  return d.toLocaleDateString('en-US', { month: 'short', day: 'numeric', timeZone: 'UTC' });
}
---

<BaseLayout
  title="Articles — Mimico Design House"
  description="Insights on UX, HCI, AI, and design strategy from the Mimico Design House team."
>
  <div class="wrap">
    <div class="page-hd">
      <h1>Articles</h1>
      <p class="lead">Research, AI, systems thinking, and practice — perspectives from the studio.</p>
    </div>

    <div class="article-grid">
      {articles.map(article => (
        <a href={`/articles/${article.slug}/`} class="art-card">
          <div class="art-thumb">
            {article.data.heroImage && (
              <img src={article.data.heroImage} alt="" loading="lazy" decoding="async" />
            )}
          </div>
          <div class="art-body">
            <div class="tag">{article.data.tag}</div>
            <h2>{article.data.title}</h2>
            {article.data.summary && <p>{article.data.summary}</p>}
            <div class="art-foot">
              <span class="dt">{fmtDate(article.data.date)}</span>
              <span class="arr">→</span>
            </div>
          </div>
        </a>
      ))}
    </div>
  </div>
</BaseLayout>
