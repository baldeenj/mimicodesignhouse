[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Serve the Tina admin SPA for all /admin/* routes
[[redirects]]
  from = "/admin/*"
  to = "/admin/index.html"
  status = 200
